# Renderer regression inputs

These former catalogue records are retained only as fixed inputs for the existing SVG regression fixtures. They are not part of the public bullet catalogue or copied into the website build. Their original provenance remains attached to the test inputs. Public samples live in `data/bullets/`.
