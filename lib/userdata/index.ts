/**
 * `userdata` defines personal records and their export format without a framework dependency.
 * The IndexedDB adapter lives in the app so this module remains usable without browser APIs.
 */

export * from './types';
export * from './transfer';
export * from './store';
