/**
 * `measure2d` - measurement over the shapes rather than pictures of them: the powder space and
 * its volumes, and whether a loaded round fits a chamber.
 */

export * from './interior';
export * from './fit';
