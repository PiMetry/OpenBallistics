/**
 * `data` contains reference tables, currently propellants.
 * The app's cartridge and bullet loaders also consume JSON generated into `site/src/lib/`
 * by `site/scripts/build-index.mjs`.
 */

export * from './powders';
