/**
 * `geom` provides profiles and SVG primitives without depending on other library modules.
 * This barrel exposes the module's exports for direct use and for the `geometry` facade.
 */

export * from './profile';
export * from './svg';
